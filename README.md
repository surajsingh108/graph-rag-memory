# RAG_memory
Implementation of RAG on LLM. Using RAG as a short term memory and a sketch pad. The RAG is also treated as a limited memory where the LLM treats it as a limited resource and summarises when it hits this limit. 
